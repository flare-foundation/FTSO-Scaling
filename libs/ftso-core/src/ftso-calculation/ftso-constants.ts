// Constants
/**
 * Maximum value for 2^256 used for modulo operation so that the result is always 256-bit unsigned number.
 */
export const MAX_2_256 = 2n ** 256n;
/**
 * Minimal required number of non-benched voters for a random to be considered secure.
 */
export const NON_BENCHED_RANDOM_VOTERS_MIN_COUNT = 2;
